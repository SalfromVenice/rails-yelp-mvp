<h1>Restaurants</h1>
<% @restaurants.each do |rest|%>
<ul>
  <li><%= link_to rest.name, restaurant_path(rest) %> | <%= rest.category %> <%= rest[@reviews] %></li>
</ul>
<% end %>
<%= link_to "+ add a restaurant", new_restaurant_path, class: 'btn btn-primary' %>
